

def printRepeating(a,size):
    for i in range(0,size):
        if a[abs(a[i])]>0:
            a[abs[a(i)]]= -a[abs(a[i])]
        print(a[abs(a[i])],end=" ")



from collections import defaultdict
number = defaultdict(int)
number['one'] = 1
number['two'] = 2
print(number['three'])


arr = [1, 2, 3, 1, 3, 6, 6]
arr_size = len(arr)

printRepeating(arr, arr_size)